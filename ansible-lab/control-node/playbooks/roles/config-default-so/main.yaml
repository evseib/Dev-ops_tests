- name: Upgrade of all packages of SO
 yum:
  name: '*'
  state: "latest"

- name: Configure Ansible lab hosts
  lineinfile:
    dest: /etc/hosts
    state: present
    line: "{{ item }}"
  loop:
    - 192.168.1.2 control-node
    - 192.168.1.3 app01
    - 192.168.1.4 db01